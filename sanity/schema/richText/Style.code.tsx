import { GrCode } from "react-icons/gr"

export const code = {
    name: "code",
    title: "Code Block",
    type: 'code', 
    icon: GrCode, 
    options: {
        withFilename: true,
        highlightedLines: true,
    }
}
